# This repository is not a distributable package. This file exists to record two
# facts a reader would otherwise have to guess: the interpreter floor, and the
# fact that the dependency set is empty.

[project]
name = "ares-case-study"
version = "0.1.0"
description = "Contract-first integration model for the ARES partner interfaces."
requires-python = ">=3.13"
dependencies = []
